# Panel listen
PORT=8080
HOST=0.0.0.0

# HTTP Basic auth (panel login)
ADMIN_USER=admin
ADMIN_PASS=changeme

# RCON config
RCON_HOST=127.0.0.1
RCON_PORT=25575
RCON_PASSWORD=12KaliRoot12

# rate limits / safety
RCON_QUEUE_CONCURRENCY=1
RCON_RETRY_MS=1500

# Optional: X-Forwarded-For support if behind Cloudflare tunnel / reverse proxy
TRUST_PROXY=1

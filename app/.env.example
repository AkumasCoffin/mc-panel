# Panel auth (HTTP Basic)
PANEL_USER=admin
PANEL_PASS=changeme

# Bind
HOST=0.0.0.0
PORT=8080

# RCON
RCON_HOST=127.0.0.1
RCON_PORT=25575
RCON_PASS=change-me

# Optional trust proxy for Cloudflare tunnel / reverse proxy
TRUST_PROXY=1

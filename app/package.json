{
  "name": "mc-rcon-webgui",
  "version": "1.6.0",
  "private": true,
  "description": "Minecraft RCON WebGUI (dark modern panel, live updates, schedules, bans, enriched UI)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "postinstall": "node -e \"try{require('fs').mkdirSync('public',{recursive:true})}catch(e){}\""
  },
  "dependencies": {
    "basic-auth": "^2.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "rcon-client": "^4.3.6",
    "sqlite3": "^5.1.7"
  }
}
